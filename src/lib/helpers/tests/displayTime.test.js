import { displayTime, timeRemaining } from "../index";

const oneSecond = 1;
const tenSeconds = 10;
const oneMinute = 60;
const tenMinutes = 600;
const oneHour = 3600;

displayTime();
timeRemaining();
